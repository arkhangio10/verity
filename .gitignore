# dependencies
node_modules/

# builds
.next/
out/
dist/
*.tsbuildinfo

# environment — never commit secrets
.env
.env.*
!.env.example
!.env.deploy.example

# deploy — credenciales y artefactos temporales del script de despliegue
scripts/deploy/.env.deploy
scripts/deploy/.last-deploy-url
scripts/deploy/.verity-src.tar.gz

# misc
coverage/
.DS_Store
npm-debug.log*

# dev/runtime logs
*.log

# only the root README is versioned — ignore every other markdown file
*.md
!/README.md
